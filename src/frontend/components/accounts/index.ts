import { AccountsRoot } from "./AccountsRoot";
import { AccountRow } from "./AccountRow";

export { AccountRow, AccountsRoot };

customElements.define("accounts-root", AccountsRoot);
customElements.define("account-row", AccountRow);
